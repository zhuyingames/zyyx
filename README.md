# zyyx

## Install

```bash
npm install -g zyyx
```

## Use

Create `res.json` in target directory

```json
{
  "assets": {
    "name": "url"
  }
}
```

Open terminal in target directory and run command `zyyx`
